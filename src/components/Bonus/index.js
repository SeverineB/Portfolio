import React from 'react';
import PropTypes from 'prop-types';

const Bonus = ({  }) => (
  <div className="bonus">
    <h2 className="bonus-title">Bonus</h2>
    <p></p>
  </div>
);

Bonus.propTypes = {
 : ,
};
export default Bonus;
